dist: trusty
sudo: required

language: rust

before_install:
- sudo apt-get update -qq
- [ "${TARGET}" = "asmjs-unknown-emscripten" ] && sudo apt-get install -qq emscripten && emcc -v

script:
  - cargo build --target $TARGET

env:
  - TARGET=asmjs-unknown-emscripten
